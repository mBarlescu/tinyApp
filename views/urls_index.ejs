<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
</head>
<body>
   <% include ./partials/_header %>
   <form method="GET" action="/urls/new">
     <button>Create a new URL</button>
   </form>
    <ul>
      List of URLS
      <hr/>
      <% for(var key in urls) { %>
        <li>ShortURL: <%= key %> | Long URL: <%= urls[key] %></li>
        <a href="<%=urls[key]%>"> <%= urls[key] %> </a>

        <form method="POST" action="/urls/<%= key %>/delete" >
            <button>delete</button>
        </form>

        <form method="GET" action="/urls/<%= key %>">
            <button>edit</button>
        </form>

      <% } %>
    </ul>
</body>
</html>


